# 更新日志

## Mio 0.2 — 2026-09-12

相较公开版 **v0.1.0**，本版重点是让 Mio 在保持日常聊天体验的同时，通过独立 Agent 工作区执行多步骤任务，并改善自动记录、环境安装和任务恢复的可靠性。

### 新增功能

- **独立 Agent 工作区与持久任务**：模型可根据目标选择已接入工具，读取结果后继续下一步；支持暂停、继续、取消、执行确认、后台生成等待与任务状态恢复。日常聊天可携带上下文和附件转交 Agent。
- **ComfyUI 创作工具**：接入图片、视频和抠图任务。支持导入 API JSON 及可转换的普通画布 JSON、识别常用输入参数、选择默认工作流，以及检查节点、模型和源文件是否齐备。
- **工作流自主研究**：模型可读取工作流结构、节点定义、允许访问的插件说明与源码；联网开启时可查阅公开参考资料。理解结果带来源与版本缓存，资料变化或过期后重新研究。
- **本地选择与文件工具**：通过文件/文件夹选择器指定 ComfyUI、工作流及授权资料目录；支持本机工作流发现、授权目录内文件搜索、文档读取和版本化成果输出。
- **分模式模型策略与运行记录**：为 Agent、日记、主动联系等模式配置模型策略，任务页查看运行时间线、失败原因和执行记录。
- **自动日记恢复**：增加独立日记模型、漏写日期检查、失败状态与重试；有记录的历史日期可以补写，没有事实素材的日期不会凭空生成。

### 体验优化

- 聊天和任务分开：日常窗口侧重对话，Agent 窗口侧重工具和执行过程；移除重复的独立创作表单入口。
- Agent 设置统一为完整设置页，按模型与推理、任务执行、文件权限、创作环境、工作流、界面显示分类，支持搜索。
- 任务执行收为输入框上方的一行目标状态，点击才展开详情；暂停、继续和取消仍可直接操作。
- 点击聊天图片后完整居中显示，周围压暗虚化；支持缩放、保存原图、Esc 关闭与窄屏适配。
- 任务预算限制默认关闭，用户可按需启用；工具标签与输入提示更简洁。
- 本地视觉区分下载完成、等待激活、运行可用和失败状态，并提供安装日志与修复入口。

### 重要修复

- 修复主动消息、启动问候和夜间收尾绕过已选择模型的问题，增加有界重试、去重与过期检查。
- 修复自动日记失败被兼容响应掩盖、漏写日期未发现，以及自动生成与手工编辑并发覆盖的问题。
- 修复本地视觉与已有 Ollama 的进程、端口和模型目录冲突；Mio 使用独立运行实例，并识别外部占用。
- 修复任务取消、会话删除和隐私暂停后残留续跑或关联生成未取消，以及重复提交、重启回执和长工具结果处理等问题。
- 修复模型策略优先级、聊天转交附件丢失、失败时遗留空会话，以及切换会话后转交提示串入其他对话的问题。
- 加强资料目录边界、输出文件校验、设置并发保存、数据维护和备份恢复的保护。
- 补齐发布检查与 CI 测试覆盖；修复仅在生产压缩包中出现的图片背景虚化失效。
- 修复 Windows 短路径或不同分隔符导致 NapCat/QQ 运行状态误判；工作流回归测试改用独立样本，不再依赖开发电脑上的 ComfyUI 文件。

### 升级与使用说明

- Windows 用户下载 `Mio-0.2.0-Windows-x64-Setup.exe`；开发者下载 `Mio-source-0.2.0.zip`。两者均附 SHA-256 校验文件。
- 从 0.1 升级前建议在 Mio 内创建完整备份。安装向导选择 **沿用原有数据** 并指定旧数据目录；选择全新目录会进入首次设置，旧记录不会自动显示在新目录中。
- 首次启动会按需升级本地数据库结构。不要将升级后的数据库直接交给旧版使用；回退时使用升级前的备份。
- 公开包不包含个人对话、日记、密钥、私人人格、音色训练素材或 ComfyUI 模型权重。ComfyUI、所需插件/模型及其他可选服务仍需自行配置。

### 当前边界

- Agent 的效果取决于所选模型及已接入工具；本版没有通用终端或任意修改项目代码的能力。
- 工作流研究缓存表示模型依据资料形成的理解，不等于工作流已成功执行或生成质量已通过验证。不同第三方节点和画布结构仍可能需要调整。
- 已发送给远程服务的请求可能无法撤销或退费；无法确认取消的任务会保留未知状态。
- Windows 安装器尚未代码签名。跨硬件、不同声卡/显卡、可选模型下载和长时间连续使用仍需更多实际反馈。

## Mio 0.1 — 2026-08-21

首个公开版本，提供对话、多模型、长期记忆、日记与回顾、主动联系、QQ、语音、Live2D、屏幕观察以及本地数据备份与隐私控制。
