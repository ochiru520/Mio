# 澪 Agent 0.5.0 验收矩阵

## 自动化发布门禁

- [x] 后端完整单元/接口测试通过：`370 tests OK`。
- [x] 桌面启动器测试通过：`17 tests OK`。
- [x] Vue 单元测试 `4` 项、生产构建和高危依赖审计通过。
- [x] Live2D 模型、锁定、菜单测试和高危依赖审计通过。
- [x] 新建、重命名、删除对话和上下文连续性通过。
- [x] 已覆盖的 API 超时、鉴权拒绝、空占位回复和断线异常不会产生空消息或重复请求；供应商真实 `429` 仍按现场状态处理。
- [x] 记忆新增、冲突、归档/删除和群聊/私聊隔离通过。
- [x] 日记跨天、信息不足、已有日记保护和周记日期边界通过；当前版本没有独立月记生成器。
- [x] 迁移账本、迁移前备份、完整备份、校验、恢复和回退通过。
- [x] 首次启动新用户、无模型用户与旧用户兼容通过。
- [x] 隐私暂停会停止观察和声音，并可恢复暂停前设置。
- [x] 干净源码包不含密钥、私人数据、日志、缓存、音色和个人资产。
- [x] `git diff --check`、忽略规则和安装资源哈希通过。

## 安装版现场验收

- [x] 隔离全新数据进入五步向导；正式版已有数据升级不进入向导。
- [x] 正式安装版创建、重命名和删除临时对话通过，删除后未留下验收数据。
- [x] 设置页创建完整备份并显示校验通过；下载和恢复链路由接口回归覆盖，未用真实数据执行覆盖恢复。
- [ ] QQ 四层状态真实，掉线诊断明确；登录失效只提示扫码，不宣称自动绕过。
- [ ] TTS 关闭、NapCat 关闭、断网、睡眠唤醒、强退和重启后主应用可用。
- [ ] 桌宠拖动、置顶、穿透、气泡、双击对话和窗口缩放不影响其他软件。
- [ ] 屏幕观察桌面、浏览器、B站、Unity 和普通窗口连续切换 20 次无灰屏。
- [x] `480x600`、`1280x720` 和 `2560x1440` 无页面级横向溢出；此前隔离验收另覆盖 `640x720`、`1920x1080`。

## 本次发布证据

- 正式安装版本：`0.5.0`，安装目录 `D:\澪Agent`，运行数据与程序目录分离。
- 安装版主程序、内置 Vue `index.html`、Live2D `app.asar` 和桌宠可执行文件与 release SHA-256 一致。
- 根地址返回 `307 -> /agent-app/`；`/health`、首次启动、隐私、启动数据和迁移接口返回 `200`。
- 安装版迁移状态为 `v1 / v1`、无待执行迁移；新建完整备份显示校验通过。
- 正式版重新启动后连续 35 秒采样 7 次，进程、`8000` 监听和健康接口持续正常。
- 干净源码包：`澪Agent-source-0.5.0.zip`。
- 源码包 SHA-256 以同目录的 `澪Agent-source-0.5.0.zip.sha256.txt` 为准，避免源码包内容引用自身哈希。

## 必须长期或指定硬件验收

- [ ] DX11、DX12、独占全屏、HDR 和双显示器捕获。
- [ ] 游戏期间 CPU/GPU/RAM/VRAM、FPS、网络和语音延迟记录。
- [ ] 真实 QQ 私聊/群聊、主动消息同步和连续登录状态。
- [ ] 连续真实使用 7 天，确认自动日记、主动联系、备份轮换和日志增长。

自动测试不能替代以上硬件、账号和长期验收。发布说明必须把未完成项列为已知限制，不能写成已经通过。
